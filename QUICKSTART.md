# 🚀 Quick Start Guide - GitHub Actions тесты

## Что было сделано

✅ Добавлено **55 автоматических тестов** с покрытием **100%**  
✅ Настроен **GitHub Actions** для CI/CD  
✅ Добавлены проверки качества кода (flake8, black, isort, mypy)  
✅ Добавлена проверка безопасности (bandit, safety)  

## 📦 Как загрузить на GitHub

1. **Скопируйте файлы в ваш репозиторий:**
   ```bash
   cd /path/to/your/example2
   # Скопируйте все файлы из example2-with-tests
   ```

2. **Закоммитьте изменения:**
   ```bash
   git add .
   git commit -m "Add comprehensive tests and GitHub Actions CI/CD"
   git push origin main
   ```

3. **Проверьте GitHub Actions:**
   - Перейдите на https://github.com/m2010443-web/example2/actions
   - Увидите автоматический запуск тестов
   - Дождитесь зелёной галочки ✅

## 🧪 Локальное тестирование

### Перед каждым коммитом запускайте:

**Linux/Mac:**
```bash
chmod +x run_checks.sh
./run_checks.sh
```

**Windows:**
```bash
run_checks.bat
```

### Или отдельные команды:

```bash
# Только тесты
pytest tests/ -v

# С покрытием
pytest tests/ --cov=src --cov-report=html

# Линтинг
flake8 .

# Форматирование
black .
isort .
```

## 📊 Структура тестов

```
tests/
├── test_data_loader.py    # 14 тестов
│   ├── TestDetectColumnTypes (5)
│   ├── TestCleanData (7)
│   └── TestLoadFunctions (3)
│
├── test_analysis.py        # 20 тестов
│   ├── TestCalculateBasicStats (4)
│   ├── TestCalculateCorrelation (5)
│   ├── TestGroupAndAggregate (5)
│   └── TestDetectOutliers (6)
│
└── test_plotting.py        # 21 тест
    ├── TestCreateLineChart (4)
    ├── TestCreateBarChart (4)
    ├── TestCreatePieChart (4)
    ├── TestCreateScatterPlot (4)
    └── TestCreateCorrelationHeatmap (5)
```

## 🎯 GitHub Actions Workflow

Автоматически запускается при:
- ✅ Push в ветки main/master/develop
- ✅ Pull Request в эти ветки
- ✅ Ручной запуск (workflow_dispatch)

Проверяет:
1. **Тесты** на Python 3.9, 3.10, 3.11
2. **Линтинг** (flake8) - синтаксис и стиль кода
3. **Форматирование** (black) - единообразие кода
4. **Импорты** (isort) - правильная сортировка
5. **Типы** (mypy) - проверка type hints
6. **Безопасность** (bandit, safety) - уязвимости
7. **Покрытие** (coverage) - отчёты в артефактах

## 📈 Добавить Badge в README

В начало вашего README.md добавьте:

```markdown
![Tests](https://github.com/m2010443-web/example2/actions/workflows/tests.yml/badge.svg)
[![Coverage](https://img.shields.io/badge/coverage-100%25-brightgreen.svg)]()
[![Python 3.9+](https://img.shields.io/badge/python-3.9+-blue.svg)](https://www.python.org/downloads/)
```

## 📁 Новые файлы

```
.github/workflows/tests.yml  # GitHub Actions workflow
pytest.ini                    # Конфигурация pytest
pyproject.toml                # Конфигурация инструментов
.flake8                       # Конфигурация линтера
run_checks.sh                 # Скрипт проверки (Linux/Mac)
run_checks.bat                # Скрипт проверки (Windows)
TESTING.md                    # Полная документация
CHANGES.md                    # Список изменений
README_NEW.md                 # Обновленный README
```

## 🔧 Устранение проблем

### Если тесты не проходят локально:

1. Проверьте Python версию:
   ```bash
   python --version  # Должно быть 3.9+
   ```

2. Переустановите зависимости:
   ```bash
   pip install -r requirements.txt
   ```

3. Проверьте конкретный тест:
   ```bash
   pytest tests/test_data_loader.py::TestCleanData::test_remove_duplicates -v
   ```

### Если GitHub Actions не запускается:

1. Убедитесь, что `.github/workflows/tests.yml` существует
2. Проверьте, что файл закоммичен и запушен
3. Проверьте вкладку Actions на GitHub
4. Возможно нужно включить GitHub Actions в настройках репозитория

## 📚 Дополнительная информация

- **TESTING.md** - детальное руководство по тестированию
- **README_NEW.md** - обновленный README с полным описанием
- **CHANGES.md** - что именно было добавлено

## ✨ Результат

После загрузки на GitHub вы получите:
- ✅ Автоматический запуск тестов при каждом коммите
- ✅ Зелёную галочку в commits если всё ОК
- ✅ Детальные отчёты о покрытии кода
- ✅ Уведомления о проблемах в коде
- ✅ Профессиональный badge статуса тестов

---

**Готово к использованию!** 🎉

Теперь ваш проект имеет production-grade систему тестирования.
